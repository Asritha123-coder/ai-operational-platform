def load_tickets():
    return [
        "Login failed after deployment",
        "Server response slow in evening",
        "Login failed for multiple users",
        "Database connection timeout"
    ]
